En esta carpeta está el código compilado de la utilidad (recuerda que está compilado con .NEYT 5.0 Preview 8).
Además del ejecutable (Compilar NETCore WinF.exe) están las DLL de colorar (gsColorearNET) y la de compilar (CompilarCore) también compiladas.<br>
CompilarCore.dll está compilada para .NET Core 3.1 y gsCompilarNET está compilada para .NET Standard 2.0.<br>
<br>
<b>NOTA:</b><br>
CompilarCore la publicaré también en gitHub aunque cambiaré el nombre para que siga con la misma nomenclatura que las demás: gsCompilarCore.<br>
Esa utilidad está escrita en C# ya que la empecé a modificar a partir de código que ya estaba en C# y otros ejemplos encontrados en la red, también en C#.<br>
Ya sabes que en Visual Basic cada vez va quedando menos cosas por la red... Salvo las que yo publico... ;-)<br>
<br>
<br>
Guillermo<br>
